# Relationship App Integration

Add this module to the Home Advocacy website via the main site shell or subpath routing. Client assets are served from `relationship-app/client/`. Server endpoints are under `/api/relationship`.

## Checklist
- Add route/mount
- Configure auth + household verification
- Verify privacy policy mentions relationship data
- Test encrypted transport + access control
